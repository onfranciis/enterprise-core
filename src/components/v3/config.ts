import { IFeatures } from "../../types/config.types";
import DarkModeV3 from "./DarkMode";
import PlayGroundV3 from "./PlayGround";

const FeaturesV3: IFeatures = {
  DarkMode: DarkModeV3,
  PlayGround: PlayGroundV3,
};

export default FeaturesV3;
